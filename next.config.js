/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "images.apilist.fun",
      "lh3.googleusercontent.com",
    ],
  },
};

module.exports = nextConfig;
